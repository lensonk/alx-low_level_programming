#include <stdio.h>
/**
 * main- print all single digits in base 10
 * and end with a new line
 * Return: always 0
 */
int main(void)
{
	int y;

	for (y = 0; y <= 9; y++)

	printf("%d", y);
	printf("\n");
	return (0);
}

