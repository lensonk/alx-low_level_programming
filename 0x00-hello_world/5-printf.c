#include <stdio.h>
main(void)
{
	/**
	 * print with proper grammer, but the outcome is a piece of art
	 * followed by a new line
	 * usin printf function
	 * should return 0
	 */
	printf("with proper grammer, but the outcome is a piece of art\n");
	return (0);
}
