#include <stdio.h>
/**
 * main function-print a string
 */
int main(void)
{
	/**
	 * print with proper grammer, but the outcome is a piece of art
	 * followed by a new line
	 * using printf function
	 *
	 * Return: print the string 'with proper grammer, but the outcome is a piece of art'
	 */
	printf("with proper grammer, but the outcome is a piece of ar,\n");
	return (0);
}
