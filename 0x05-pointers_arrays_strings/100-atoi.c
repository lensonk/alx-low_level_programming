#include "main.h"

/**
 * _atoi-converts a string to an integer
 * @s:String to convert
 * Return: return i value or  0 if no numbers in the string
 */

int _atoi(char *s)
{
	int i = _atoi(s);

	printf(" %d\n", i);

	if (i != 0)

	return (i);

	else

	return (0);
}
