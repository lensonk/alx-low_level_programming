#include "main.h"

/**
 * islower-Function to check whether lowercase character
 *
 * @c:Checked if lowercase character
 *
 * Return: 1 if lower 0 if not
 */

int islower(int c)
{
	if (c >= 'a' && c <= 'z')

	return (1);

		else

	return (0);
}
