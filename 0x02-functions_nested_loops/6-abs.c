#include "main.h"

/**
 * _abs-Computes absolute value of an integer
 * @i:The integer to be computed.
 *
 * Return: always return 0
 *
 */
int _abs(int i)
{

	i = (i - 0);

	if (i > 0)

	return (i);

	else
	return (-i);
}
